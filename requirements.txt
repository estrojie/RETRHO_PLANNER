# Runtime dependencies for RHO Planner.
# Python 3.11 is used for all release builds.
#
# PySide6 is intentionally held at 6.9.3 for Linux x86_64 because its wheel
# targets manylinux_2_28 (glibc 2.28+), which supports a wider range of Linux
# distributions than newer Qt for Python releases.
PySide6==6.9.3

numpy>=2.1,<3
pandas>=2.2,<3
matplotlib>=3.9,<4
scipy>=1.14,<2
Pillow>=11,<13
requests>=2.32,<3

astropy>=7.1,<9
astroquery>=0.4.11,<0.5
astroplan==0.10.1
astropy-iers-data>=0.2025.1

openpyxl>=3.1,<4
certifi>=2025.1
tzdata>=2025.1
